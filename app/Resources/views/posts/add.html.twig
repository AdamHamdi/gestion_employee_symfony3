{% extends 'base.html.twig' %} {% block title %}Ajouter article {% endblock %} {% block body %}
<div class="col-sm-8 podt mx-auto mt-5">
    <div class="card">
        <h1 class="card-header ">Ajouter un Post</h1>
        <div class="card-body ">

            {{ form_start(form) }} {{ form_widget(form) }}
            <a href="" type="submit" class="btn btn-success">Ajouter</a> {{ form_end(form) }}
        </div>
    </div>
</div>
{% endblock %}